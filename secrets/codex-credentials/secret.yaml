name: codex-credentials
description: OpenAI Codex CLI ChatGPT subscription OAuth state
required: false
hint: Copy ~/.codex/auth.json after running `codex login`; the broker persists token refreshes automatically.
keys:
  - name: auth.json
    format: json
    hint: Complete native Codex auth.json file
verify:
  run: true
